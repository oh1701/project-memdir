[project]
name = "project-memdir"
version = "1.0.10"
description = "Project-scoped long-term memory hooks for Codex and Claude Code"
requires-python = ">=3.11"
readme = "README.md"
license = { file = "LICENSE" }

[tool.pytest.ini_options]
python_files = ["test_*.py"]
